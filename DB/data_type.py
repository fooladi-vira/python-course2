# INTEGER: این نوع داده برای ذخیره اعداد صحیح  INTEGER یا INT 
# REAL: این نوع داده برای ذخیره اعداد اعشاری  REAL یا FLOAT یا DOUBLE 
# TEXT: این نوع داده برای ذخیره رشته‌های متنی TEXT یا VARCHAR 
# BLOB:  تصاویر، فایل‌ها و غیره استفاده می‌شود.
# NULL: 


# CREATE TABLE users (
#     id INTEGER PRIMARY KEY,
#     name TEXT,
#     age INTEGER
# );

#SELECT * FROM users WHERE age > 30;


# UPDATE users
# SET age = 30
# WHERE name = 'John';


# UPDATE users
# SET age = 30, name = 'John Smith'
# WHERE id = 1;